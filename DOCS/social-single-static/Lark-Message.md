# Lark Message — SINGLE STATIC

Sao chép toàn bộ nội dung dưới đây để dán vào comment của Lark task.

---

```text
[BÀN GIAO TASK: SINGLE STATIC WORKFLOW]

Đính kèm:
1. Architecture.md — kiến trúc workflow single-static, dependency, setup máy mới, quyền truy cập và điều kiện nghiệm thu.
2. HDSD.md — runbook setup, chuẩn bị input, chạy role tuần tự, QA và xử lý lỗi.
3. social-single-static-WORKFLOW-DIAGRAM.png — sơ đồ 9:16 1080×1920 theo goal quy chuẩn.
4. PRODUCTION/goal/[social]_[single-static].md — workflow authority; do đây là bàn giao architecture-only và chưa có campaign image test độc lập, file goal là bằng chứng instruction chính.
5. Lark-Message.md — nội dung comment bàn giao này để tái sử dụng/lưu vết.

Workflow authority: PRODUCTION/goal/[social]_[single-static].md.
Runtime access, env.local, NOTION_TOKEN và service entitlement image-gen được cấp qua kênh nội bộ được phê duyệt; không nằm trong Git hoặc Lark attachments.

Nhờ Giám sát kiểm tra: khả năng setup lại workflow, Notion → Ticket.md/node/GOAL.md mapping, copy tiếng Việt qua nested agy, render final image đúng ticket, và manifest/Notion writeback đúng `Submit to Review`. Chỉ publish khi Ticket.md cấp quyền.
```
