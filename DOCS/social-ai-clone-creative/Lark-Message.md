# Lark Message — AI CLONE CREATIVE

Sao chép toàn bộ nội dung dưới đây để dán vào comment của Lark task.

---

```text
[BÀN GIAO TASK: AI CLONE CREATIVE]

Đính kèm:
1. Architecture.md — kiến trúc workflow ai-clone, dependency, setup máy mới, quyền truy cập và điều kiện nghiệm thu.
2. HDSD.md — runbook setup, chuẩn bị input/reference, reverse-prompt, adapt + render, QA và xử lý lỗi.
3. social-ai-clone-creative-WORKFLOW-DIAGRAM.png — sơ đồ 9:16 1080×1920 theo goal quy chuẩn.
4. mutant-big-green-cloned-creative.png — final image test 1254×1254; traceability nằm trong campaign unit `2026-08-13-clone-test` cùng Ticket.md, node/ và manifest.json.
5. Lark-Message.md — nội dung comment bàn giao này để tái sử dụng/lưu vết.

Workflow authority: PRODUCTION/goal/[social]_[ai_clone_creative].md.
Base workflow: PRODUCTION/goal/[social]_[single-static].md.

Runtime access, env.local, NOTION_TOKEN và service entitlement image-gen được cấp qua kênh nội bộ được phê duyệt; không nằm trong Git hoặc Lark attachments.

Nhờ Giám sát kiểm tra: khả năng setup lại workflow, Notion → Ticket.md/node/GOAL.md mapping, reference ingestion + reverse-prompt JSON, structural fidelity ≥ 9/10, cặp reference/JSON reusable trong Brand Template, và manifest/Notion writeback đúng `Submit to Review`. Lưu ý: test hiện có `status = test_review_required`, chưa publish vì chưa có `notion_page_id` và ticket approval.
```
