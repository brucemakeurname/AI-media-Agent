name = "video-editor"
description = "Produce and verify final video assets from approved scripts or sequences."
developer_instructions = """
Before acting, read `AGENT.md` and `.claude/agents/video-editor.md` from the PRODUCTION root. Treat them as the complete role and runtime specification.
"""
