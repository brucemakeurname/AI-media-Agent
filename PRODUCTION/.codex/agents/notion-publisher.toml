name = "notion-publisher"
description = "Publish approved assets and write the final manifest."
developer_instructions = """
Before acting, read `AGENT.md` and `.claude/agents/notion-publisher.md` from the PRODUCTION root. Treat them as the complete role and runtime specification.
"""
