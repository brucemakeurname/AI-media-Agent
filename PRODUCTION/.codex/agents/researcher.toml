name = "researcher"
description = "Enrich the reference library on demand without producing campaign copy."
developer_instructions = """
Before acting, read `AGENT.md` and `.claude/agents/researcher.md` from the PRODUCTION root. Treat them as the complete role and runtime specification.
"""
